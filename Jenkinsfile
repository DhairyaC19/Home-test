pipeline {
    agent any
    
    stages {
        stage('Build') {
            steps {
                // Build your project (e.g., compile code, generate artifacts)
            }
        }
        stage('Test') {
            steps {
                // Test your project (e.g., run unit tests, integration tests)
            }
        }
        stage('Deploy') {
            steps {
                // Deploy your project (e.g., to a staging or production environment)
            }
        }
    }
}
